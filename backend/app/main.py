from fastapi import FastAPI
from fastapi.routing import APIRoute
from starlette.middleware.cors import CORSMiddleware

from app.api.main import api_router
from app.core.config import settings


def custom_generate_unique_id(route: APIRoute) -> str:
    return f"{route.tags[0]}-{route.name}"


tags_metadata = [
    {
        "name": "login",
        "description": """
Авторизация и восстановление доступа пользователей.
Здесь реализована логика входа в систему по протоколу OAuth2 с выдачей JWT‑токена, проверка работоспособности токена, восстановление пароля по email и его последующая смена.
Также доступен эндпоинт для просмотра HTML‑содержимого email‑сообщения восстановления пароля.
""",
    },
    {
        "name": "categories",
        "description": """
Работа с категориями позволяет создавать новые рубрики, получать список всех категорий, обновлять существующие и удалять ненужные категории по идентификатору. Эти операции обеспечивают структурирование видеоконтента и упрощают навигацию по тематикам для пользователей видеохостинга.
""",
    },
    {
        "name": "me",
        "description": """
Эндпоинты «me» предназначены для управления собственным профилем пользователя: получение информации о текущем пользователе, обновление личных данных и пароля, а также удаление учётной записи. Это позволяет пользователю безопасно контролировать свои данные и параметры доступа к сервису.
""",
    },
    {
        "name": "users",
        "description": """
Администрирование пользователей включает регистрацию и создание новых учётных записей, чтение списка всех пользователей и информации по конкретному пользователю, обновление и удаление профилей, а также операции социального характера: подписка/отписка от других пользователей, получение списков подписчиков и подписок, а также просмотр истории просмотров. Эти эндпоинты обеспечивают управление сообществом и анализ пользовательской активности на платформе.
""",
    },
    {
        "name": "videos",
        "description": """
Управление видеоконтентом включает загрузку роликов в формате видеофайлов, получение списка доступных видео, обновление метаданных и удаление по идентификатору, а также специализированные операции: получение персональной ленты рекомендованных видео, потоковая трансляция ролика, запрос миниатюры, переключение лайка, регистрация просмотра и получение статистики по просмотрам и лайкам. Все эти эндпоинты обеспечивают полный цикл работы с видео на платформе, от загрузки и публикации до анализа пользовательского взаимодействия.
""",
    },
    {
        "name": "utils",
        "description": """
Набор вспомогательных утилит включает проверку работоспособности сервиса и тестовую отправку email: эндпоинт health-check позволяет убедиться, что все компоненты API активны и готовы к работе, а test-email выполняет пробную отправку письма для проверки корректности настроек почтового сервера. Эти утилиты помогают оперативно отслеживать состояние системы и гарантировать доставку уведомлений.
""",
    },
]

app = FastAPI(
    title=settings.PROJECT_NAME,
    openapi_url=f"{settings.API_V1_STR}/openapi.json",
    generate_unique_id_function=custom_generate_unique_id,
    openapi_tags=tags_metadata,
)

# Set all CORS enabled origins
if settings.all_cors_origins:
    app.add_middleware(
        CORSMiddleware,
        allow_origins=settings.all_cors_origins,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

app.include_router(api_router, prefix=settings.API_V1_STR)
